# Jekyll configuration for GitHub Pages
title: Pipeline Modelling Language
description: A visual modelling language for data pipelines
baseurl: "/pipeline-modelling"
url: "https://sparkboxx.github.io"

# Build settings
markdown: kramdown
highlighter: rouge
theme: just-the-docs

# Include/exclude files
exclude:
  - readme.md

# Plugins
plugins:
  - jekyll-feed
  - jekyll-sitemap

# Collections
collections_dir: .

# Default layouts
defaults:
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "page"

